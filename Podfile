# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
platform :ios, '16.0'
target 'Meeat' do
  pod 'GoogleMaps'
  pod 'GooglePlaces'
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Meeat

  target 'MeeatTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MeeatUITests' do
    # Pods for testing
  end

end
